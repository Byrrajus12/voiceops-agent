services:
  target-service:
    build: ./target-service
    ports:
      - "8080:8080"
    environment:
      BROKEN: ${BROKEN:-false}
      FAILURE_RATE: ${FAILURE_RATE:-0.8}

  approval-server:
    build: ./approval-server
    ports:
      - "9000:9000"
